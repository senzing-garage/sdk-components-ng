import { SzResolvedEntityData } from './resolved-entity-data';
import { SzEntityDetailSectionData } from '../../entity-detail-section-data';

export interface SzEntityData {
  resolvedEntity: SzResolvedEntityData;
  // possibleMatches: EntityDetailSectionData
  // discoveredRelationships: EntityDetailSectionData
  // disclosedRelationships: EntityDetailSectionData
  // relatedEntities: RelatedEntities;
}
